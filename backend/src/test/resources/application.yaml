spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

application:
  security:
    jwt:
      secret-key: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970"
      expiration: 86400000
      refresh-token:
        expiration: 604800000
  cloudinary:
    cloud-name: "test-cloud"
    api-key: "test-key"
    api-secret: "test-secret"
    user-avatar-folder: "test-folder"
    user-transformation: "test-transform"
  clarivate:
    base-url: "https://api.clarivate.com/apis/wos-journals/v1"
    api-key: "test-clarivate-key"
    sync:
      cron: "-"
      trigger-token: "test-sync-token"
      years-to-keep: 2
      rate-limit-per-second: 4

